services:
  - type: web
    name: film-calendar
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: TMDB_ACCESS_TOKEN
        sync: false
      - key: OMDB_API_KEY
        sync: false
